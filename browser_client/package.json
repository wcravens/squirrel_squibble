{
  "name": "browser_client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.1.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "sass": "^1.53.0"
  },
  "scripts": {
    "bootstrap": "markserv bootstrap-5.2.0-beta1-examples",
    "start": "export REACT_APP_NAME='Squirrel Squibble' && export REACT_APP_VERSION='Pre-Alpha 0' && export REACT_APP_BRANCH=$(git branch)  && export REACT_APP_BUILD=$(git rev-parse --short HEAD) && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
